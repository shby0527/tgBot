<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.shby0527.tgbot.dao.TagToImgMapper">
    <resultMap id="BaseResultMap" type="com.github.shby0527.tgbot.entities.TagFoImgKey">
        <id column="imgId" jdbcType="BIGINT" property="imgid"/>
        <id column="tagId" jdbcType="BIGINT" property="tagid"/>
    </resultMap>

    <resultMap id="TagPopular" type="com.github.shby0527.tgbot.entities.TagPopular">
        <result property="tagId" column="tagId" jdbcType="BIGINT"/>
        <result property="tag" column="tag" jdbcType="VARCHAR"/>
        <result property="count" column="c" jdbcType="BIGINT"/>
    </resultMap>

    <insert id="insert">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jul 19 12:04:09 CST 2022.
        -->
        insert into `tag_fo_img` (`imgId`, `tagId`)
        values (#{imgid,jdbcType=BIGINT}, #{tagid,jdbcType=BIGINT})
    </insert>
    <insert id="insertSelective">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jul 19 12:04:09 CST 2022.
        -->
        insert into `tag_fo_img`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="imgid != null">
                `imgId`,
            </if>
            <if test="tagid != null">
                `tagId`,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="imgid != null">
                #{imgid,jdbcType=BIGINT},
            </if>
            <if test="tagid != null">
                #{tagid,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>


    <!-- ### 以上代码由MBG + CommentPlugin自动生成, 生成时间: 2022-07-19 12:04:09 ### -->


    <!-- 下面可以写业务代码!!! -->
    <select id="tagsIdToImageId" resultType="java.lang.Long">
        SELECT DISTINCT imgId
        from tag_fo_img
        where tagId in
        <foreach collection="tagIds" nullable="false" item="tagId" open="(" close=")" separator=",">
            #{tagId, jdbcType=BIGINT}
        </foreach>
        <if test="notTags != null">
            and tagId not in
            <foreach collection="notTags" nullable="false" item="ntgId" open="(" close=")" separator=",">
                #{ntgId, jdbcType=BIGINT}
            </foreach>
        </if>
        order by imgId desc
        limit 500
    </select>
    <select id="getTopOfTags" resultMap="TagPopular">
        SELECT tagId,
        (select it.tag from info_tags it where it.id = tag_fo_img.tagId) tag,
        count(1) c
        from tag_fo_img
        group by tagId
        order by 3 desc
        limit #{limit}
    </select>

    <select id="getImagesTags" resultMap="com.github.shby0527.tgbot.dao.InfoTagsMapper.BaseResultMap">
        SELECT it.* from info_tags it
        left join tag_fo_img tfi on it.id = tfi.tagId
        where tfi.imgId = #{imgId}
    </select>

    <select id="getImageKey" resultMap="BaseResultMap">
        SELECT *
        from tag_fo_img
        where tagId = #{tagId, jdbcType=BIGINT}
        <if test="nextId != null and nextId != 0">
            and imgId &lt; #{nextId, jdbcType=BIGINT}
        </if>
        order by imgId desc
        limit 1
    </select>
</mapper>